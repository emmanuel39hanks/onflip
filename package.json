{
  "name": "flip-asp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Flip — agent-callable prediction-market execution ASP. Buy a single Polymarket/Kalshi outcome or combine up to 6 into one leveraged parlay, funded by one x402 payment (USDT on X Layer).",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "packageManager": "pnpm@9.15.9",
  "dependencies": {
    "viem": "^2.55.4"
  }
}